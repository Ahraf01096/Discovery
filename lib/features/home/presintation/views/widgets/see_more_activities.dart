import 'package:flutter/material.dart';

class SeeMoreActivities extends StatelessWidget {
  const SeeMoreActivities({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(

    );
  }
}
